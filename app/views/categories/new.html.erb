
<div class="form_new mt-5">
<h2>Create a new category</h2>

<%= simple_form_for (@category) do |f| %>
<%= f.input :name, placeholde: "The category name...." %>
<%= f.input :photo, as: :file,input_html: { class: 'form-control' } %>
<%= f.submit class:"btn btn-info" %>
<% end %>
</div>