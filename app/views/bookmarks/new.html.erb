
<div class="form_new mt-5">
<h2>Add a recipe</h2>

<%= simple_form_for [@category, @bookmark] do |f| %>
<%= f.association :recipe, collection: @recipes, prompt: "Choose a Recipe..." %>

<%= f.input :comment, placeholder: "Comment" %>
<%= f.submit "Save", class: "btn btn-info" %>
<% end %>
</div>
